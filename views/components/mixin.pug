// breadcrumb
mixin RouteNav(nav)
    div.route-nav
        h2 #{nav}

mixin RouterLink(obj)
    ul
        each item in obj
            li
                a(href=""+item.link) #{item.text}

mixin asideNav(obj,activeIndex)
    ul#aside
        each item,index in obj
            li(class=(activeIndex==index)?'active':'')
                a(href=""+item.link)
                    i.icon(class=[item.icon])
                    |#{item.text}